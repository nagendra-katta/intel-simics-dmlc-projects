/*
  © 2025 Intel Corporation

  This software and the related documents are Intel copyrighted materials, and
  your use of them is governed by the express license under which they were
  provided to you ("License"). Unless the License provides otherwise, you may
  not use, modify, copy, publish, distribute, disclose or transmit this software
  or the related documents without Intel's prior written permission.

  This software and the related documents are provided as is, with no express or
  implied warranties, other than those that are expressly stated in the License.
*/

dml 1.2;

header %{
#include <simics\util\bitcount.h>
%}

import "simics/base-types.dml";

extern uint32 bit_count32(uint32 x);
extern uint32 bit_count64(uint64 x);
extern uint32 count_leading_zeros32(uint32 x);
extern uint32 count_leading_zeros32_const(uint32 x);
extern uint32 count_leading_zeros32_nonzero(uint32 x);
extern uint32 count_leading_zeros64(uint64 x);
extern uint32 count_leading_zeros64_const(uint64 x);
extern uint32 count_leading_zeros64_nonzero(uint64 x);
extern uint32 count_trailing_zeros32(uint32 x);
extern uint32 count_trailing_zeros32_const(uint32 x);
extern uint32 count_trailing_zeros32_nonzero(uint32 x);
extern uint32 count_trailing_zeros64(uint64 x);
extern uint32 count_trailing_zeros64_const(uint64 x);
extern uint32 count_trailing_zeros64_nonzero(uint64 x);
extern bool is_power_of_2(uint64 x);
extern int log2_32(uint32 x);
extern int log2_64(uint64 x);
